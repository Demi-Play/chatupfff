import React from 'react';
import Message from './Message';

const ChatMessages = () => {
    return (
        <div>
            <Message text='Make multiple functions that will return the sum, difference, modulus, product, quotient, and the exponent respectively' />
        </div>
    );
}

export default ChatMessages;
